<resources>

    <!-- Base application theme. -->
    <style name="SmartTvTheme" parent="Theme.AppCompat" >
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorAccent">@color/colorCyanAccent700</item>
    </style>

    <style name="SmartTvTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar"/>

    <style name="SmartTvTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
